SELECT
      CASE
            WHEN IM_ITEM.TRK_METH = 'G'
            THEN IM_ITEM.TRK_METH
      END AS 'BIGCOMMERCE',

REPLACE(REPLACE(IM_ITEM.TRK_METH,'G','SKU'),'N','PRODUCT') AS 'Item Type'
FROM IM_ITEM
